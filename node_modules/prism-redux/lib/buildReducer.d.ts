import { Action, Handler, Unwrapper } from './types';
export interface UnwrapperHandlerPair<S> {
    unwrapper: Unwrapper;
    handler: Handler<S>;
}
declare var _default: <S>(handlers: UnwrapperHandlerPair<S>[], initialState: S) => (state: S, action: Action) => S;
export default _default;
